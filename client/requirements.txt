# --- speech ---
faster-whisper>=1.1
# onnxruntime-gpu 1.23.x is the last line built against CUDA 12 — the same CUDA that CTranslate2
# (faster-whisper) needs, so one set of nvidia-*-cu12 wheels serves both. 1.24+ moved to CUDA 13.
onnxruntime-gpu==1.23.*; platform_system != "Darwin"
onnxruntime>=1.20; platform_system == "Darwin"
nvidia-cudnn-cu12>=9; platform_system != "Darwin"
nvidia-cublas-cu12>=12; platform_system != "Darwin"
nvidia-cuda-runtime-cu12>=12; platform_system != "Darwin"
nvidia-cufft-cu12>=11; platform_system != "Darwin"
nvidia-curand-cu12>=10; platform_system != "Darwin"
nvidia-cuda-nvrtc-cu12>=12; platform_system != "Darwin"
sounddevice>=0.5
soundfile>=0.12
numpy>=1.26
num2words>=0.5.13
transformers>=4.40
huggingface_hub>=0.23
# --- networking / UI ---
websockets>=12
httpx>=0.27
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
python-dotenv>=1.0
# --- computer control ---
mss>=9
pyautogui>=0.9.54
pyperclip>=1.9
psutil>=5.9
pillow>=10
